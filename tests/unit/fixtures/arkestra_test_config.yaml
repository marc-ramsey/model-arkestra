default:
  model-start-port: 18000
  model-ports: 4
  warmup-time: 10

macros:
  ctx-size: 16384

backends:
  default: vulkan-radv
  vulkan-radv:
    args: -hf ${CHECKPOINT}

runners:
  default: process

models:
  test-model:
    model: dummy/dummy:Q4_K_M
    args:
      temp: 0.7
